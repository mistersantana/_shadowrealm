#include <stdio.h>

int main () {

	// arithmetic operators = + - * / % ++ --

	int x = 10;
	float y = 2;
	float z = 0;

	// z = x + y;
	// z = x - y;
	// z = x * y;
	// z = x / y;
	// z = x % y;
	
	x++;
	x+=3;

	printf("%f", z);

	
	return 0;
}
